import 'package:flutter/material.dart';

class FriendFoeBody extends StatelessWidget {
  const FriendFoeBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Friend or Foe Body'),
    );
  }
}