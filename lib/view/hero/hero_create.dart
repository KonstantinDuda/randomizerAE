import 'package:flutter/material.dart';

class HeroCreatePage extends StatelessWidget {
  const HeroCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}